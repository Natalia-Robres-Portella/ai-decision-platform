fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.5.0
pydantic-settings>=2.1.0

# Database
sqlalchemy[asyncio]>=2.0.0
asyncpg>=0.29.0
alembic>=1.13.0

# Vector DB
qdrant-client>=1.7.0

# LlamaIndex RAG stack
# llama-index-core 0.10.x uses pydantic v1 internals incompatible with Python 3.14.
# 0.12+ uses pydantic v2 natively. llama-index-vector-stores-qdrant will be
# added back in the query step once a Python-3.14-compatible release lands.
llama-index-core>=0.12.0
llama-index-embeddings-openai>=0.3.0
tiktoken>=0.5.0

# AI / embeddings
openai>=1.10.0

# Document processing
pypdf>=4.0.0

# File upload support (required by FastAPI for multipart/form-data)
python-multipart>=0.0.9

# Utilities
python-dotenv>=1.0.0
httpx>=0.25.0
structlog>=23.0.0

# Testing
pytest>=7.4.0
pytest-asyncio>=0.23.0
pytest-cov>=4.1.0
