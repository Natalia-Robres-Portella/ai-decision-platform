-r requirements.txt

# Pin to the same version as .github/workflows/ci.yml (format output differs across ruff releases).
ruff==0.8.6

pytest==8.3.5
pytest-asyncio==0.24.0
pytest-cov==6.0.0
httpx==0.27.2
aiosqlite==0.20.0
